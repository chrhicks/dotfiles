alias ls='ls -aG'
alias ll='ls -laG'
alias reload='source ~/.zshrc'
alias backup='mkdir -p ~/.backup && cp -R ~/.dotfiles ~/.apibuilder ~/.zsh_history ~/.aws ~/.chicksrc ~/.flow ~/.gnupg ~/.npmrc ~/.oh-my-zsh ~/.ssh ~/.tools ~/.backup'